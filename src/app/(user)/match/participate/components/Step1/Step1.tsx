"use client";

import styles from "./Step1.module.scss";

type Step1PropsType = {};

const Step1 = (props: Step1PropsType) => {
  return <h1 className={styles.step1}>Step 1</h1>;
};

export default Step1;
