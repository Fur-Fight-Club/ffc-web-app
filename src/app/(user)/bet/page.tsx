"use client";

import styles from "./page.module.scss";

type BetPageProps = {};

const BetPage = (props: BetPageProps) => {
  return <h1 className={styles.betPage}>Bet Page</h1>;
};

export default BetPage;
